import 'package:flutter/material.dart';

class CoadColors {
  // Classic "Modeling in Colors" Archetype colors
  static const Color pink = Color(0xFFFFCCFF);    // Moment-Interval
  static const Color yellow = Color(0xFFFFFF99);  // Role
  static const Color green = Color(0xFFCCFFCC);   // Thing, Party, Place
  static const Color blue = Color(0xFF99CCFF);    // Description

  // Modern slightly darker variants for text/borders
  static const Color pinkDark = Color(0xFFFF99FF);
  static const Color yellowDark = Color(0xFFFFCC00);
  static const Color greenDark = Color(0xFF99CC99);
  static const Color blueDark = Color(0xFF6699CC);
}
